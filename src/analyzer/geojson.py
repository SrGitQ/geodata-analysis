from dataclasses import dataclass

@dataclass
class GeoJson:
    """
        Class to generate the needed data required by clients the raw data will
        be analyzed and transformed to be used in the client side.

        # Parameters

        # Output

        # Use
    """
    var_:str
    
